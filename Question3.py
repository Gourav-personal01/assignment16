# Redirect in Flask is used when we want to route the api to different API Endpoint which means the methods is execute with a API then we write the redirect method to redirect that api to different endpoint.

# Redirect is specially used in order to avoid the downtime of some URI's.